//
   Created by dsds on 10/13/15.
extends admin
block view
  a.button.button-primary.float-right(href="/admin/groups/new") New Group
  h1 Groups
  if message && message.length
    p.success #{message}
  p #{data.length} group(s) in total.
  table.u-full-width
    thead
      tr
        th №
        th= __("groupname")
        th= __("created at")
    tbody
      each groupdata, index in data
        tr
          td #{index + 1}
          td
            a(href="/admin/groups/" + groupdata._id) #{groupdata.name}
          td #{ObjectId(groupdata._id).getTimestamp().toLocaleString()}